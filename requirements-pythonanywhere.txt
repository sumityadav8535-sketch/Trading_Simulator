# Slim install for PythonAnywhere free tier (~512MB disk).
# Skips scikit-learn/scipy (~150MB+) so the site fits; F&O ML probs
# simply stay unavailable until you upgrade disk or install sklearn later.
#
#   pip install --no-cache-dir -r requirements-pythonanywhere.txt

Django>=5.0,<6.0
numpy>=1.26.0,<2.3
pandas>=2.2.0,<3
plotly>=5.24.0,<6
python-dateutil>=2.9.0
yfinance>=0.2.40
whitenoise>=6.6.0
